[Statedef -4]

ignoreHitPause{
	let condition = var(21)=1;
}

ignoreHitPause if $condition{
	text{
		text: "Fortis931";
		font: 0;
		align: ifelse(teamside=1,1,-1);
		pos: ifelse(teamside=1,27.5,320-27.5),55;
		color: ifelse(p2name="Stiyl Magnus" && teamside=2,128,256),0,ifelse(p2name="Stiyl Magnus" && teamside=2,256,0);
		scale: 0.75,0.75;
	}
}