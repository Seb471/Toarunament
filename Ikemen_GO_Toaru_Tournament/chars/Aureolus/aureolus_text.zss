[Statedef -4]

ignoreHitPause{
	let condition = var(20)=1 && !ishelper;
}

ignoreHitPause if $condition{
	text{
		text: "Honos628";
		font: 0;
		align: ifelse(teamside=1,1,-1);
		pos: ifelse(teamside=1,27.5,320-27.5),55;
		color: 256,216,0;
		scale: 0.75,0.75;
	}
}